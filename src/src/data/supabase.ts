// ponytail: Supabase stub — aktifkan saat kredensial Poltekkes datang
// Ganti null dengan createClient saat .env siap; repositories tetap pakai Dexie dulu.

export const supabase: null = null
// TODO: import { createClient } from '@supabase/supabase-js'
// export const supabase = createClient(import.meta.env.VITE_SUPABASE_URL!, import.meta.env.VITE_SUPABASE_ANON_KEY!)
